<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@3.6.0/fonts/remixicon.css" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class=" w-full h-screen">
        <div class="flex items-center justify-between w-full h-12">
            <img class="w-12" src="https://pngimg.com/d/pinterest_PNG62.png" alt="">
            <a class="md:hover:bg-black md:hover:text-white md:px-3 md:py-2 md:rounded-3xl " href="/home">Home</a>
            <a class="md:hover:bg-black md:hover:text-white md:px-3 md:py-2 md:rounded-3xl " href="/save">Save</a>
            <a class="md:hover:bg-black md:hover:text-white md:px-3 md:py-2 md:rounded-3xl " href="/create">Create</a>
            <i class="ri-search-line md:hidden"></i>
            <input class="md:w-[800px] hidden md:block h-1 rounded-3xl p-5 outline-none text-lg bg-zinc-200 text-zinc-500" type="text" placeholder=" Search">
            <i class="ri-notification-fill md:text-3xl"></i>
            <i class="ri-message-2-fill md:text-3xl"></i>
            <a href="/profile">
                <div class="w-8 h-8 rounded-full border-2 mr-2 md:mr-0 overflow-hidden">
                    <img class="w-full h-full object-cover" src="../images/profileImg/<%=foundUser.image%>" alt="">
                </div>
            </a>
            <i class="ri-arrow-down-s-line md:text-3xl hidden md:block" ></i>
        </div>
       <div class="h-[580px] w-full  flex items-center justify-center ">
            <div class="md:w-[400px] h-[500px]  flex items-center flex-col p-5 gap-5">
                <div class="relative w-[200px] h-[200px] bg-zinc-500 rounded-full overflow-hidden pointer-events-none">
                    <img id="profileImage" class="w-full h-full object-cover " src="/images/profileImg/<%= foundUser.image%>" alt="">
                </div>
                <i id="pencil" class="ri-pencil-line pen absolute z-50 hover:cursor-pointer md:top-[42%] md:right-[45%] top-[32%] right-[35%] bg-zinc-200 px-1 rounded-full text-2xl"></i>
                <form action="/edit" method="post" enctype="multipart/form-data" >
                    <input  type="file" class="upload-img hidden" name="image">
                    <input class="w-full p-2 text-lg mb-5 rounded-md outline-none border-2" type="text" placeholder="Usename" value="<%= foundUser.username%>" name="username">
                    <input class="w-full p-2 text-lg mb-5 rounded-md outline-none border-2" disabled type="text" placeholder="Email" value="<%= foundUser.email%>" name="email">
                    <input class="w-full p-2 text-lg mb-5 rounded-md outline-none border-2" type="text" placeholder="Bio" value="<%= foundUser.bio%>" name="bio">
                    <button class="px-4 py-2 rounded-3xl bg-blue-500 text-white font-semibold" >Update</button>
                </form>
            </div>
       </div>
    </div>
<% include ./partials/footer.ejs%>

<script>
    document.querySelector("#pencil").addEventListener("click",function(){
        document.querySelector(".upload-img").click();
      })
      document.querySelector(".upload-img").addEventListener("change",()=>{
        profileImage.src = URL.createObjectURL(document.querySelector(".upload-img").files[0])
      })
</script>